import styled from 'styled-components';

export const EmptySolutionsText = styled.div`
  color: #ccc;
  font-style: italic;
  text-align: center;
  margin: 16px;
  pointer-events: none;
  flex: 1;
`;
